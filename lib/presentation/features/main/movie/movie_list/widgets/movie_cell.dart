import 'package:flutter/material.dart';

class MovieCell extends StatelessWidget {
  const MovieCell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Movie Cell');
  }
}
